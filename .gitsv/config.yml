versioning:
  update-major: []
  update-minor: [feat]
  update-patch: [build, ci, chore, docs, fix, perf, refactor, revert, style, test]
  ignore-unknown: false

tag:
  pattern: "v%d.%d.%d"
  filter: "v*"

release-notes:
  sections:
    - name: Changes
      section-type: commits
      commit-types: ["", build, ci, chore, docs, feat, fix, perf, refactor, revert, style, test]

branches:
  disable-issue: true
  skip: []
